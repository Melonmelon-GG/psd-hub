# 复制本文件为 .env.local 后按需修改（.env.local 不会进入版本库）。

# 后端 API 基址。
# 留空 = 同源访问（生产模式推荐，由后端 SERVE_STATIC 托管本前端）。
# 开发模式下留空亦可：Vite 会把 /api 代理到 http://127.0.0.1:4000（见 vite.config.ts）。
# 若前后端分域部署，请填完整来源且不要带尾斜杠，例如：https://api.example.com
VITE_API_BASE=

# 上传令牌，对应契约 §0.3 的 x-upload-token 头，仅用于 POST /api/projects。
# 留空即可：GET /api/config 返回 uploadTokenRequired=false 时前端不会发送空令牌头。
# 若服务端配置了 UPLOAD_TOKEN，也可以留空此处，改由页面上的令牌输入框填写
# （保存在浏览器 localStorage，键名 psd-hub-token）。
VITE_UPLOAD_TOKEN=
